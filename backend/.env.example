# ==============================================
# ENVIRONMENT VARIABLES FOR RENDER DEPLOYMENT
# ==============================================
# Copy these to Render Dashboard > Environment Variables

# MongoDB Atlas Connection String
# Get this from: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/sponsozbd?retryWrites=true&w=majority

# Server Port (Render sets this automatically, but add for safety)
PORT=10000

# JWT Secret Key (Use a strong random string!)
# Generate one at: https://randomkeygen.com
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long

# JWT Token Expiration
JWT_EXPIRE=7d

# Node Environment
NODE_ENV=production
